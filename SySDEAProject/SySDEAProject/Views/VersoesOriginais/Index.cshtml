@model IEnumerable<SySDEAProject.Models.VersaoOriginal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dataCadastro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numero)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dataCadastro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numero)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idVersaoOriginal }) |
            @Html.ActionLink("Details", "Details", new { id=item.idVersaoOriginal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idVersaoOriginal })
        </td>
    </tr>
}

</table>
